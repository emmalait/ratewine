<p id="notice"><%= notice %></p>

<h1>Wineries</h1>

<p>Number of wineries: <%= @wineries.count %> </p>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Year</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @wineries.each do |winery| %>
      <tr>
        <td><%= link_to winery.name, winery %></td>
        <td><%= winery.year %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<p><%= link_to('New winery', new_winery_path) if current_user %> </p>
<p><%= link_to 'List of wines', wines_path %> </p>
